collections_config:
  blog:
    path: blog
    _inputs:
      featured_image:
        type: image
        comment: Helpful message here
    _select_data:
      colors:
        - Red
        - Blue
  portfolio:
    path: portfolio
editor:
  default_path: /company/
paths:
  uploads: .src/image