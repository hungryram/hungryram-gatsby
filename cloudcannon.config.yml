collections_config:
  blog:
    path: blog
    _inputs:
      featured_image:
        type: image
        comment: Helpful message here
      is_post:
        type: checkbox
        comment: is this a blog?
  portfolio:
    path: portfolio
  profile:
    path: data/
editor:
  default_path: /company/
paths:
  uploads: .src/image